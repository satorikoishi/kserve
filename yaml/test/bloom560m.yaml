apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: "bloom560m"
spec:
  predictor:
    model:
      modelFormat:
        name: pytorch
      storageUri: pvc://task-pv-claim/bloom560m
      resources:
        limits:
          cpu: "2"
          memory: 64Gi
          nvidia.com/gpu: "1"
        requests:
          cpu: "2"
          memory: 64Gi
          nvidia.com/gpu: "1"