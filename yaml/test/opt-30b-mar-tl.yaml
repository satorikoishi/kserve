apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: "opt"
spec:
  predictor:
    model:
      modelFormat:
        name: pytorch
      storageUri: pvc://task-pv-claim/opt-30b-mar-tl
      resources:
        limits:
          cpu: "2"
          memory: 128Gi
          nvidia.com/gpu: "2"
        requests:
          cpu: "2"
          memory: 128Gi
          nvidia.com/gpu: "2"